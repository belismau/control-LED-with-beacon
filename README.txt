--- Instructions to get started ---

1 - Navigate to the project root and run npm install in the terminal to download all the dependencies
2 - Once done, run node index.js to start the webserver
3 - Navigate to localhost:8080 (or localhost:5000 depending on the port)
4 - Now you can start sending/receiving messages with the broker